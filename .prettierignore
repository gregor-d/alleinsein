# Single source of truth for what prettier formats — keep include/exclude
# rules here, not in .pre-commit-config.yaml. Prettier is run over the whole
# repo (`prettier .`) and this file narrows it down.

# Ignore everything, then un-ignore only the frontend/static source dir.
# /*
# !/frontend/
# !/docs/
# !/README.md
# Within the included dir, skip data files (not hand-formatted source).
*.geojson
